services:
  pg:
    image: postgres
    environment:
      POSTGRES_DB: demo_auth
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      PGDATA: /var/lib/postgresql/data
    ports:
        #слева внешний порт/ внутренний порт контейнера
      - "5435:5432"
    volumes:
      - ./postgresdata:/var/lib/postgresql/data